import { ENV_CONFIG } from "../shared/env";
import { runtimeInfo } from "../shared/runtime-info";
import { ApiService } from "./api.service";



const monitorApiService = new ApiService(
    '',
    {
      baseUrl: ENV_CONFIG.monitorApiUrl,
    },
);

const peerManagerApiService = new ApiService(
  '',
  {
    baseUrl: ENV_CONFIG.peerApiUrl,
  },
);


export {
  monitorApiService,
  peerManagerApiService
}