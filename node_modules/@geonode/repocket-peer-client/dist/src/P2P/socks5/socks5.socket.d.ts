export declare class Socket5Handler {
    socket: any;
    private logger;
    private port;
    private localAddress;
    private dns;
    private userPassAuthFn;
    constructor(socket: any, options?: any);
    init(): void;
    consume(): Promise<unknown>;
    reply(rep: any, address?: any): void;
    request(data: any): Promise<any>;
    private createConnection;
    handle(data: any): Promise<void>;
}
